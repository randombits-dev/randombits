---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/home/Hero.astro";
import ArticleList from "../components/article/ArticleList.astro";
import EmailMe from "../components/EmailMe.astro";
---

<BaseLayout title="Random Bits Software Engineering" description="Software engineering guides, articles, and projects.">
  <Hero img="i1.svg">
    <div class="text-[75px]" >random</div>
    <div class="text-[150px]">bits</div>
    <div class="text-[60px] px-6 leading-[0.8]">software</div>
    <div class="text-[48px] px-6 leading-[0.8]">engineering</div>
  </Hero>

  <!--<div class="text-center">-->
  <!--  <h1 class="text-[60px] m-0">websites, enterprise applications, technology consulting, recruiting</h1>-->
  <!--  <h2 class="text-[38px]">from a simple website to a custom enterprise app</h2>-->
  <!--  <p class="text-lg">A business website should be modern, yet simple. Too many websites use fancy animation </p>-->

  <!--</div>-->

  <div class="container pt-24">
  <h1>Design. Build. Ship.</h1>
    <div class="text-2xl font-bold">Web apps, mobile apps, technology consulting, and more.</div>
  <p class="text-xl">Whether your building something custom, enhancing an existing app, or just need advice, I can help. </p>
  <div class="pt-10 flex">
    <a href="/services" class="btn-primary">View Services</a>
    <a href="/about" class="btn-secondary ml-10">About Me</a>
    <!--<EmailMe/>-->
  </div>
  </div>

  <div class="container pt-24 pb-48">
    <h1>Featured Articles</h1>
    <ArticleList limit={6}/>
  </div>


  <!--<div class="mx-5 md:mx-10 mb-20">-->
  <!--  <h1 class="text-center">Recent Projects</h1>-->
  <!--  <ToolList limit={2}/>-->
  <!--  <a class="button" href="/projects">See All Projects</a>-->

  <!--  <h1 class="mt-30 text-center">Articles</h1>-->
  <!--  <ArticleList/>-->
  <!--</div>-->


  <div aria-disabled="true"
       class="rb-background-bits absolute px-1 py-1 top-0 h-[500px] sm:h-[600px] leading-tight tracking-tighter text-justify overflow-hidden z-0">
    <!--<RandomBits client:only="solid-js"/>-->
  </div>
</BaseLayout>

<style>

  h1 b {
    background: linear-gradient(90deg, #0531a2 0%, #031768 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .rb-background-bits {

    //margin-top: 55px;
    //color: var(--landing-bits);
    //background: linear-gradient(var(--back-0) 4%, var(--landing-bits) 25% 75%, rgba(255, 255, 255, 0%));
    opacity: 0;
    color: #03314d;
    font-size: 20px;
    //background: linear-gradient(var(--back-header) 4%, #025388 25% 75%, var(--back-header));
    //-webkit-background-clip: text;
    //-webkit-text-fill-color: transparent;
    font-family: "Courier New", serif;
    font-weight: 900;
    letter-spacing: -3px;
    //border-bottom: 1px solid var(--landing-bits);

  }

  .list-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 30px;
    background-color: var(--back-2);
    color: var(--color-back);
  }

  //a.button {
  //  max-width: 500px;
  //  margin: 30px auto;
  //}

  .other-area {
    max-width: 1200px;
    margin: 0 auto;
    padding: 30px;
    text-align: center;
    background-color: var(--back-2);
  }

  .other-area a.button {
    max-width: 500px;
    margin: 0 auto;
  }

</style>

<script>
  const html = [...Array.from({length: 10000})].map(() => {
    return `<span>${Math.random() < .5 ? 0 : 1} </span>`;
  }).join(' ');
  document.querySelector('.rb-background-bits').innerHTML = html;

  // document.querySelector('.rb-background-bits').addEventListener('mousemove', (e) => {
  //   console.count('mousemove')
  //   if (e.target.tagName === 'SPAN') {
  //     e.target.style.fontWeight = 'bold';
  //
  //   }
  // });
</script>
